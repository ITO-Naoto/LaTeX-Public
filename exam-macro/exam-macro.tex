%目標：解答の表示・非表示を切り替えるマクロを作る。
\documentclass{jlreq}
%パッケージ読みこみ
\usepackage{amsmath}
\usepackage{fancybox}
\usepackage{xcolor}

%マクロ定義開始
%・問題+解答＋解答答の注釈（部分点など）を表示（解答が一行で書けないものは，枠付き箱で囲って表示する。）
\newif\if main
\if main
\newcommand{\question}[2]{}
\newcommand{\answer}[2]{}
\newcommand{\answer-annotation}[2]{}
\fi%main

%・問題のみ（解答分のスペースを開ける）
\newif\if question-only
\if question-only
\newcommand{\question}[2]{}
\newcommand{\answer}[2]{}
\fi%question-only

%・問題のみ（解答分のスペースを開けない）
\newif\if question-only-nospace
\if question
\newcommand{\question}[2]{}
\fi%question-only-nospace

%・解答のみ
\newif\if answer-only
\if question
\newcommand{\answer}[2]{}
\newcommand{\answer-annotation}[2]{}
\fi%answer-only

\begin{document}
\begin{enumerate}
    \item 次の計算をせよ。
    \begin{enumerate}
        \item $1+1$
        \item $2\times 4$
    \end{enumerate}
\end{enumerate}
\end{document}